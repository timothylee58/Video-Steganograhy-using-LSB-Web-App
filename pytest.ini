[pytest]
# Only collect modules named test_*.py. Repo ships scripts like stress_test.py (stress_* suite),
# which match pytest's default *_test.py pattern and must not be imported as tests.
python_files = test_*.py
testpaths = tests
