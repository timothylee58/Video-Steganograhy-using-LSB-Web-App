# fly.toml app configuration file generated for video-steganograhy-using-lsb-web-app on 2026-05-24T05:11:07Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'video-steganograhy-using-lsb-web-app'
primary_region = 'sin'

[build]

[env]
  FLASK_ENV = 'production'
  MAX_UPLOAD_MB = '500'
  OUTPUT_FOLDER = '/app/uploads/outputs'
  SOCKETIO_ASYNC_MODE = 'threading'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0

  [http_service.concurrency]
    type = 'connections'
    hard_limit = 25
    soft_limit = 20

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024

[[mounts]]
  source = 'vidstega_data'
  destination = '/app/uploads'
  initial_size = '10gb'

[checks]
  [checks.health]
    port = 8080
    type = 'http'
    interval = '30s'
    timeout = '10s'
    grace_period = '60s'
    method = 'GET'
    path = '/health'

[[metrics]]
  port = 8080
  path = '/metrics'
